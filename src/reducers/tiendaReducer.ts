const initialState = {
  products: [
    { name: "product1", price: 133 },
    { name: "product2", price: 232 },
  ],
  carrito: [],
};

const reducer = (state: Object = initialState, action: Object) => {
  return state;
};

export default reducer;
